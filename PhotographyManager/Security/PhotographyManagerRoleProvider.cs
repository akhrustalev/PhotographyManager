using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using PhotographyManager.Model;

namespace PhotographyManager.Security
{
    //public class PhotographyManagerRoleProvider
    //{
    //    public bool IsUserInRole(string userName, string roleName)
    //    {
    //        using (var photographyManagerContext = new PhotographyManagerContext())
    //        {
    //            var user = photographyManagerContext.UserProfile.SingleOrDefault(u => u.UserName == userName);
    //            if (user == null)
    //                return false;
    //            return user. != null && user.UserRoles.Select(
    //                 u => u.Role).Any(r => r.RoleName == roleName);
    //        }
    //    }

    //}
}